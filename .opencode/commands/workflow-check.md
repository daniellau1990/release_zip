---
description: Run the workflow compliance guard to check if a plan file exists
---

Run the workflow compliance guard script.

This checks if a plan file exists in `docs/plans/` for today's date. If no plan exists, you should create one before writing code.

Usage: `/workflow-check`

The guard script is at `.opencode/hooks/workflow_check.py`.
